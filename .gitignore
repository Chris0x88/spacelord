# Archived files (moved during restructure - keep structure but ignore content)
archive/
archive_internal/
legacy/
deprecated/

# Environments
.env
.env.local
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# Data & Logs (Keep empty structure but ignore contents if needed, or keep tracked if static)
# The user wants "execution_records" for AI, so we generally KEEP them or ignore them?
# "I need you to record... for training". So we should probably GITIGNORE them so they don't bloat the repo,
# but the app must generate them.
execution_records/
logs/
training_data/

# Node
node_modules/
package-lock.json
package.json

# IDEs
.vscode/
.idea/
pacman_env/
